# IDEAS

רעיונות שעלו תוך כדי בנייה ואינם ב-SPEC. לא לבנות בלי החלטה מפורשת.

## שלב 4 — פרסומות (banner ads) לגרסה החינמית

§5 ב-SPEC מזכיר "פרסומות באנר בתחתית הספרייה" בגרסה החינמית. הוחלט
עם המשתמש (סשן RevenueCat, HANDOFF.md §19) לדחות את זה למשימה נפרדת
— אינטגרציית AdMob היא native module + חשבון משלה (Google AdMob,
ad unit IDs), לא חלק מ-RevenueCat. **לא לבנות בלי החלטה מפורשת.**

## שלב 1

- **טסט ריצה על Windows.** אין סימולטור במכונת הפיתוח, ו-`expo start --web`
  נופל כי ה-worker של `expo-sqlite` לא נסרל ע"י ה-static serializer של Expo.
  אם נרצה לולאת בדיקה מקומית בלי טלפון — צריך להחליט אם מוסיפים תמיכת web
  (שינוי `web.output` + `assetExts` + headers) או מתקינים אמולטור אנדרואיד.
- **פלטפורמה כרשימה סגורה.** `PLATFORMS` ב-`src/constants/session.ts` הוא
  מערך קבוע. אם ירצו "אחר" עם טקסט חופשי — זה שינוי סכמה קטן.

## שלב 1.5 — מיפוי ארכיטיפים

- ~~**§4.4 לא מכסה משחקים קצרים מטבעם.**~~ **נסגר.** נוסף ארכיטיפ
  `arcadeSession` (10 דק', ניתן לעצירה) שממופה מז'אנרי IGDB
  `Racing`, `Fighting`, `Sport`, `Arcade`, `Pinball`.
- **ז'אנר `Platform` נשאר לא ממופה.** משחקי פלטפורמה מבוססי-שלבים
  (Celeste) הם סשן קצר וניתן לעצירה, אבל אחרים (Mario Odyssey) קרובים
  יותר ל-openWorldSandbox. אין דרך להבדיל מהתגיות, ולכן הם נשארים
  בברירת המחדל השמרנית. להחליט.
- **לא שומרים keywords ב-DB.** הארכיטיפ מחושב פעם אחת בייבוא ונשמר
  כ-`typical_session_minutes` + `interruptible` (§6). אם נשפר את טבלת
  המיפוי בעתיד, אי אפשר יהיה לחשב מחדש למשחקים קיימים בלי לפנות שוב
  ל-IGDB. חלופה: להוסיף עמודת `keywords` ל-games.
- **הפרוקסי מכסה רק IGDB.** נתיבי Steam (ResolveVanityURL + GetOwnedGames)
  ייבנו יחד עם ייבוא Steam בשלב 2, באותו שרת.

## כלי תיוג ידני (seed) — נבנה, הושלם ומחובר

`tools/tagger`, נפתח ב-http://localhost:8788 כחלק מ-`npm run dev`.
300 המשחקים המדורגים ביותר ב-IGDB, תיוג במקלדת, שמירה אחרי כל תשובה
ל-`seed/session-profiles.json`.

**הוכרע:** ה-seed מחובר. `addGameFromIgdb` (`src/db/repositories/gamesRepo.ts`)
משתמש ב-`getSeedOverride(igdbId)` (`src/lib/sessionProfile/seedOverrides.ts`)
לפני שהוא נופל לברירת המחדל של הארכיטיפ. `is_calibrated` נשאר `false`
כמו שהומלץ — §4.5 ממשיך לאסוף דיווחי סשן אמיתיים גם על 300 אלה.

**נמדד אחרי התיוג:** 43.7% אי-הסכמה (131/300) מול הניחוש המקורי. פירוט
מלא, מה תוקן בטבלת המיפוי ומה לא (ולמה) — ראה HANDOFF.md §5 ו-§7.
`seed/session-profile-tags.json` (genres/themes/keywords אמיתיים מ-IGDB
לכל 300 המשחקים) נשמר לצד ה-seed לצורך ניתוח וטסטים על נתוני אמת.

## שלב 2 — ייבוא Steam

- **העשרת ז'אנר/ארכיטיפ למשחקי Steam לפי IGDB.** `importSteamGames`
  (`src/db/repositories/steamImportRepo.ts`) מייבא כרגע בלי לחפש כל
  משחק ב-IGDB — כל משחק Steam מתחיל בברירת המחדל השמרנית של הסכמה
  (§4.4), בדיוק כמו הוספה ידנית. שיפור אפשרי: לכל משחק לחפש ב-IGDB
  לפי שם (התאמה מדויקת בלבד), ואם נמצא — למשוך genres/themes/keywords
  אמיתיים כמו בהוספה דרך IGDB. **לא נבנה עכשיו** כי: (א) עשרות/מאות
  בקשות IGDB רצופות בייבוא ראשוני מסתכנות בהתנגשות עם מגבלת הקצב
  (4/שנייה) ודורשות תזמון זהיר; (ב) התאמת שם Steam↔IGDB לא מובטחת
  (מהדורות, "GOTY", תווים מיוחדים) ועלולה לשייך משחק לפרופיל סשן
  שגוי בלי שהמשתמש ביקש את זה. §4.3 מפורש: "אל תיתקע פה".

## רעיון עתידי — הצעת משחקים חינמיים (FreeToGame API)

נבדק מול מסד המשחקים הראשי (IGDB) ונמצא לא רלוונטי כתחליף/תוספת לספרייה
הכללית — הוא מכסה רק ~400 משחקי Free-to-Play/MMO, בלי נתוני זמן-סשן
ובלי כיסוי כללי כמו IGDB. עם זאת, יכול להתאים לפיצ'ר עתידי נפרד: מצב
"אין לי משחק בבאקלוג שמתאים — תציע לי משהו חינמי להוריד עכשיו", כאפשרות
נוספת במסך "מה לשחק?".

- **מקור:** https://www.freetogame.com/api-doc — בלי אימות/API key, מוגבל
  ל-10 בקשות/שנייה. דורש קרדיט ל-FreeToGame.com (לינק פעיל) לפי תנאי השימוש.
- **נתונים זמינים:** כותרת, ז'אנר, תיאור, מפתח/מוציא לאור, פלטפורמה, תמונת
  thumbnail, קישור למשחק.
- **חסר:** נתוני זמן-סשן/completion time (בדיוק מה ש-§4.4 צריך) — אותה
  בעיה כמו עם IGDB, תצטרך מיפוי ארכיטיפים נפרד או ברירת מחדל שמרנית.
- **לא לבנות בלי החלטה מפורשת** — זה פיצ'ר חדש שלא קיים ב-SPEC היום.

(נבדק גם TheGamesDB — api.thegamesdb.net — ונפסל: מסד קהילתי מתחרה
ל-IGDB, כיסוי חלש יותר למשחקים מודרניים, בלי תועלת ברורה על מה שכבר קיים.)

## רעיון עתידי — התחברות ל-Steam דרך Steam עצמו (OpenID)

כרגע ב-`connect-steam.tsx` המשתמש מדביק ידנית SteamID64 או קישור לפרופיל.
חלופה כמו באתרים ("Sign in through Steam"): פותחים דפדפן שמפנה לדף ה-login
של Steam עצמו (Steam OpenID 2.0 — לא OAuth, אין שיתוף סיסמה), המשתמש
מתחבר שם, וסטים מפנה בחזרה עם SteamID64 מאומת.

- **דורש שרת לאימות.** אי אפשר לסמוך על הפרמטרים שחוזרים בצד הלקוח בלבד —
  צריך POST חזרה ל-Steam לאימות החתימה. `tools/dev-server.ts` רץ רק לוקאלית
  ב-LAN; יידרש endpoint מתארח (למשל Vercel function) גם בפרודקשן.
- **דורש deep link חזרה לאפליקציה** (`expo-web-browser`/`expo-auth-session`
  עם scheme כמו `playnight://steam-callback`) כדי לחזור מהדפדפן לאפליקציה
  אחרי ה-login בדף של Steam.
- זה משדרג רק את **זיהוי המשתמש** — עדיין צריך `STEAM_API_KEY` בשביל
  `getOwnedGames` (§שלב 2 מעלה), לא מחליף את שכבת ה-API.
- **לא לבנות בלי החלטה מפורשת** — תלוי בשרת מתארח שעדיין לא קיים בפרויקט.
