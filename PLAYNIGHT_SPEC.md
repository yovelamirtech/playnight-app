# PlayNight — Product Spec

> מסמך אפיון מלא לבנייה עם Claude Code.
> **שם עבודה:** PlayNight (אפשר לשנות)
> **גרסה:** 1.0 — MVP Spec

---

## 1. הרעיון בשורה אחת

**אפליקציה שעונה על שאלה אחת: "יש לי X דקות פנויות עכשיו — מה לשחק?"**

לא עוד אפליקציית מעקב (tracker). כל אפליקציות הבאקלוג הקיימות
(Backloggd, GG, SavePoint, Backloggr) בנויות סביב **תיעוד** — לרשום מה שיחקת,
לדרג, לכתוב ביקורת. PlayNight בנויה סביב **החלטה**.

### הפער בשוק (מבוסס מחקר)

| מתחרה | מה יש לו | מה חסר |
|---|---|---|
| Backloggd | קהילה גדולה, רשימות מעולות, חינמי | **אין אפליקציית מובייל** (רק אתר) |
| GG App | אפליקציה טובה, ייבוא ספריות, 100K+ הורדות | **אין נתוני זמן־השלמה**, לא שומר על איזו פלטפורמה כל עותק |
| HowLongToBeat | נתוני זמן מדויקים — הדבר הכי חשוב | זה מסד נתונים, לא כלי החלטה אישי |
| SavePoint / Backloggr | ייבוא אוטומטי, ביקורות | עדיין ממוקדי "רישום", לא "בחירה" |

**המסקנה:** אף אחת לא משלבת *זמן פנוי + מבנה הסשן של המשחק (לא זמן
כולל להשלמה!) + מצב רוח + הבאקלוג שלך* לכדי החלטה אחת מהירה.
זה החור שאנחנו נכנסים אליו — ואף מתחרה לא ממדל בכלל "כמה זמן לוקח
סשן אחד", רק "כמה סה"כ יש במשחק" (אם בכלל).

### קהל היעד

גיימר בן 22–40, עובד, עם 100–400 משחקים בספרייה על 2–4 פלטפורמות,
שיש לו 1–3 שעות פנויות בערב ומבזבז 20 דקות מתוכן על גלילה בספרייה
בלי להחליט. אנחנו קוראים לו **"גיימר עם עבודה"**.

### עקרון עיצוב מנחה

> **מהמסך הראשון ועד להחלטה: פחות מ־10 שניות ופחות מ־3 טאפים.**

כל פיצ'ר שלא משרת את זה — נדחה לגרסה עתידית.

---

## 2. Stack טכני

| רכיב | בחירה | סיבה |
|---|---|---|
| Framework | **React Native + Expo (SDK 52+)** | קוד אחד ל־iOS ו־Android, Claude Code מתמודד איתו מצוין |
| שפה | **TypeScript** | חובה — מונע חצי מהבאגים מראש |
| ניווט | **Expo Router** (file-based) | פשוט וסטנדרטי |
| State | **Zustand** | קל בהרבה מ־Redux, מספיק לגודל הזה |
| DB מקומי | **SQLite** (`expo-sqlite`) + Drizzle ORM | הספרייה חייבת לעבוד offline |
| Backend | **Supabase** (Postgres + Auth) | חינמי בהתחלה, auth מוכן, לא צריך לנהל שרת |
| Server-state | **TanStack Query** | caching ו־sync מול Supabase |
| עיצוב | **NativeWind** (Tailwind ל־RN) | מהיר לכתיבה |
| אנימציות | **Reanimated 3** + `react-native-gesture-handler` | חובה לסוואיפ |
| מסד משחקים | **IGDB API** (דרך Twitch) | 300K+ משחקים, חינמי, cover art |
| נתוני זמן | **אין! (הוסר)** — ראה §4.4 | הוחלף בפתרון בטוח יותר |
| אנליטיקס | **PostHog** (free tier) | להבין מה אנשים באמת עושים |
| מנויים | **RevenueCat** | מטפל ב־IAP של שתי החנויות במקום אחד |

### הערה חשובה על Expo

השתמש ב־**Expo Development Build**, לא Expo Go, כי צריך native modules
(RevenueCat, SQLite מתקדם). זה עדיין הרבה יותר קל מ־bare React Native.

---

## 3. מסכים — פירוט מלא

### 3.1 Onboarding (פעם אחת)

**מסך 1 — Welcome**
- לוגו + טקסט: "יש לך 200 משחקים. בוא נמצא את זה שמתאים להערב."
- כפתור: "בוא נתחיל"

**מסך 2 — חיבור פלטפורמות**
- רשימה של: Steam, PlayStation, Xbox, Nintendo Switch, Epic, GOG
- כל אחת עם כפתור "חבר"
- לינק תחתון: **"אני אוסיף ידנית"** ← חשוב! אל תחסום משתמש שלא רוצה לחבר חשבון
- Steam הוא הכי קל טכנית (API ציבורי + Steam ID) — התחל ממנו

**מסך 3 — העדפות בסיס**
- "כמה זמן פנוי יש לך בדרך כלל בערב?" → 30 דק' / שעה / 2 שעות / 3+
- "מה הפלטפורמה הראשית שלך?" → בחירה מרובה
- (שומר כברירות מחדל, ניתן לשינוי אחר כך)

**מסך 4 — ייבוא**
- אנימציית טעינה + "מייבא 247 משחקים..."
- בסיום: "מצאנו 89 משחקים שקנית ולא פתחת. בוא נתקן את זה."

---

### 3.2 מסך הבית — **"מה לשחק?"** ⭐ הלב של המוצר

זה המסך שנפתח בכל הרצה. לא ספרייה. לא feed. **מסך החלטה.**

**חלק עליון — בחירת זמן:**

```
כמה זמן יש לך?
[ 30 דק' ]  [ שעה ]  [ 2 שעות ]  [ הערב כולו ]
```

כפתורים גדולים, טאפ אחד. ברירת מחדל = מה שבחר ב־onboarding.

**חלק אמצעי — מצב רוח (אופציונלי, טאפ אחד):**

```
[ 🧠 משהו עמוק ]  [ ⚡ אקשן מהיר ]  [ 😌 רגוע ]
[ 🎯 לסיים משהו ]  [ 🆕 חדש לגמרי ]  [ 🎲 הפתע אותי ]
```

**חלק תחתון — כפתור ראשי:**

```
        [  תבחר לי משחק  ]
```

לחיצה → עובר למסך ה־Swipe.

**Empty state:** אם הספרייה ריקה — כפתור אחד גדול "חבר את Steam" או "הוסף משחק".

---

### 3.3 מסך Swipe — הבחירה

מציג **3–5 כרטיסים** (לא יותר! עומס בחירה הוא האויב).

**כל כרטיס מכיל:**
- Cover art גדול (מ־IGDB)
- שם המשחק
- **⏱ "סשן טיפוסי: ~15 דק', אפשר לעצור בכל רגע" / "סשן: ~40 דק', עדיף לא להתחיל אם אין זמן לסיים"** ← הנתון הכי חשוב (§4.4)
- 🎮 פלטפורמה (אייקון)
- 📊 "קנית לפני 2 שנים, לא נגעת" / "עצרת לפני 4 חודשים"
- ⭐ ציון קהילה (IGDB rating)
- 🏷 2–3 תגיות ז'אנר

**מחוות:**
| מחווה | פעולה |
|---|---|
| Swipe ימינה | **"זה! בוא נשחק"** → מסך אישור |
| Swipe שמאלה | "לא הערב" (מוריד עדיפות לשבוע) |
| Swipe למעלה | "לא בא לי מהמשחק הזה" (מסתיר לתמיד) |
| Tap על כרטיס | פותח פרטים מלאים |

**באג נפוץ להימנע ממנו:** הכרטיסים חייבים להרגיש מיידיים.
השתמש ב־Reanimated עם `useSharedValue` ו־`runOnJS` — לא ב־Animated הישן.

**אם עבר על כולם:** "עוד 5 אפשרויות" או "שנה את הפילטרים".

---

### 3.4 מסך אישור — "משחקים!"

- Cover גדול
- "נהנה מ־[שם משחק]!"
- 🔘 **טיימר סשן** — "התחל סשן של 45 דק'" (עם התראה עדינה בסוף)
- ✍️ שדה הערה: **"איפה עצרתי?"** ← זה קריטי (§4.2)
- כפתור: "סיימתי לשחק" → פותח מסך לוג מהיר

---

### 3.5 מסך לוג מהיר (אחרי סשן)

חייב לקחת **פחות מ־15 שניות**:
- "איך היה?" → 😍 / 🙂 / 😐 / 😴 (לא כוכבים — מהיר יותר)
- "היית יכול לעצור מתי שרצית, או שהיית נעול עד נקודה מסוימת?" → כן/לא (טאפ אחד — זה מה שבונה את נתון ה־session fit, §4.4)
- "איפה עצרת?" → שדה טקסט חופשי, עם תזכורת: "תכתוב משהו שיעזור לך בעוד חודש"
- "סיימת את המשחק?" → כן/לא
- כפתור דילוג בולט

---

### 3.6 הספרייה

- **טאבים:** משחק עכשיו | באקלוג | הושלם | על המדף | נטוש
- תצוגת רשת (grid) עם covers
- חיפוש + פילטרים: פלטפורמה, ז'אנר, זמן להשלמה, שנה
- מיון: נוסף לאחרונה, זמן להשלמה, ציון, אלפביתי
- **פיצ'ר מיוחד: "אבק"** — מיון לפי "כמה זמן זה יושב בלי שנגעת"

---

### 3.7 מסך משחק בודד

- Cover, שם, שנה, מפתח
- **הסטטוס שלך** + היסטוריית סשנים
- **"איפה עצרתי" — כל ההערות שלך, לפי תאריך** ← הפיצ'ר שאף אחד לא עושה טוב
- זמן HLTB: Main Story / Main+Extras / Completionist
- כמה זמן אתה כבר שיחקת (אם יובא מ־Steam)
- כפתורים: שנה סטטוס, הוסף הערה, הסר

---

### 3.8 Game Night (Premium) — §5

- "מי פנוי הערב?" → שולח לחברים
- מציג משחקים ש**כולם מחזיקים** ותומכים ב־co-op/multiplayer
- הצבעה מהירה: כל אחד מסמן 3 העדפות → מציג את החפיפה

---

## 4. פיצ'רים ליבה — עומק

### 4.1 מנוע ההמלצה

**זה לא ML. זו נוסחת ניקוד פשוטה.** אל תסבך.

```typescript
score =
  sessionFit     * 0.40 +   // ראה §4.4 — האם הזמן הפנוי מתאים למבנה הסשן של המשחק
  moodMatch      * 0.20 +   // התאמת ז'אנר למצב הרוח
  momentum       * 0.20 +   // התחלת משחק ולא סיימת = בונוס
  dustFactor     * 0.10 +   // קנית ולא נגעת = בונוס
  communityScore * 0.10     // דירוג IGDB

// מכפילים:
if (dismissedThisWeek) score *= 0.3
if (platformUnavailable) score = 0    // אין קונסולה בבית? אל תציע
if (userHidGame) score = 0
if (sessionFit === 0) score = 0       // אין טעם להציע משחק שפיזית לא נכנס בזמן
```

**sessionFit — הלוגיקה החשובה ביותר, ראה נוסחה מלאה ב־§4.4:**
- זמן פנוי 20 דק' + Hades (roguelike, ניתן להפרעה) → **התאמה מושלמת**
- זמן פנוי 20 דק' + משימת GTA נעולה של 40 דק' → **לא מוצע בכלל** (score=0)
- זמן פנוי שעתיים + כל משחק → כמעט הכל מתאים, sessionFit כמעט תמיד 1.0

**מיפוי מצב רוח → ז'אנרים (IGDB genre IDs):**

| מצב רוח | ז'אנרים |
|---|---|
| 🧠 משהו עמוק | RPG, Adventure, Strategy, Visual Novel |
| ⚡ אקשן מהיר | Shooter, Fighting, Racing, Hack-and-slash |
| 😌 רגוע | Simulation, Puzzle, Farming, Card, Point-and-click |
| 🎯 לסיים משחק | מתעלם מז'אנר — ממיין לפי "הכי קרוב לסיום" |
| 🆕 חדש לגמרי | רק משחקים שמעולם לא הופעלו |
| 🎲 הפתע אותי | רנדומלי משוקלל |

**חשוב:** תמיד לערבב לפחות כרטיס אחד "לא צפוי" מתוך ה־5.
המלצות צפויות מדי משעממות והמשתמש נוטש.

### 4.2 "איפה עצרתי" — הפיצ'ר הסודי

זו הבעיה שהזכרת: **שוכחים**. אף מתחרה לא עושה את זה כמו שצריך.

- אחרי כל סשן — שדה הערה חופשי
- כשחוזרים למשחק אחרי >30 יום, האפליקציה **קופצת עם ההערה האחרונה**:
  > "עצרת לפני 4 חודשים. כתבת: 'אחרי הבוס בביצה, צריך למצוא את המפתח השני'"
- אפשרות לצרף צילום מסך
- **זה הפיצ'ר שיגרום לאנשים להמליץ על האפליקציה לחברים.**

### 4.3 ייבוא ספריות

| פלטפורמה | שיטה | קושי |
|---|---|---|
| **Steam** | Steam Web API — `GetOwnedGames` עם Steam ID | 🟢 קל — **התחל מכאן** |
| Xbox | OpenXBL API (צד שלישי) | 🟡 בינוני |
| PlayStation | PSN API לא רשמי — שביר | 🔴 קשה |
| Nintendo | אין API. ייבוא ידני / סריקת צילום מסך | 🔴 קשה |
| GOG / Epic | API מוגבל | 🟡 בינוני |

**ל־MVP: Steam אוטומטי + הוספה ידנית לכל השאר.** אל תיתקע פה.

### 4.4 פרופיל סשן ("Session Fit") — הנתון שבאמת חשוב

**זה לא "כמה זמן עד שמסיימים את המשחק". זה "כמה זמן לוקח סשן אחד
במשחק הזה, וכמה גמיש הוא."** שני משחקים בני 40 שעות יכולים
להתנהג הפוך לגמרי:

| משחק | מבנה | האם מתאים ל־20 דק' פנויות? |
|---|---|---|
| Roguelike (Hades, Slay the Spire) | ריצות של 10–20 דק', כל ריצה נגמרת בפני עצמה | ✅ מעולה |
| Open-world עם fast travel וסייב חופשי (Zelda) | אפשר להיכנס ל־5 דק' ולצאת | ✅ טוב |
| Open-world עם משימות ארוכות ללא סייב באמצע (GTA story missions) | ברגע שנכנסת למשימה, אתה נעול ל־30–45 דק' | ❌ גרוע |
| משחק תחרותי מבוסס־מאץ' (Valorant, LoL) | כל משחק = 20–40 דק' קבועות, אי אפשר לצאת באמצע | ⚠️ רק אם יש בדיוק את הזמן |
| אסטרטגיה מבוססת־תורות (Civilization) | "עוד תור אחד" — נודע לגלוש הרחק מעבר למתוכנן | ❌ מסוכן ל"20 דקות" |
| JRPG עם דנג'ונים ארוכים | דנג'ון בודד יכול לקחת שעה+ בלי נקודת עצירה טובה | ❌ גרוע לזמן קצר |

**המסקנה:** אנחנו לא צריכים בכלל לדעת "כמה זמן עד הסוף". אנחנו צריכים
לדעת: **(א) כמה זמן אורך סשן טיפוסי במשחק הזה, ו-(ב) האם אפשר לצאת
בכל רגע או שצריך להגיע לנקודת עצירה.**

**איך אוספים את זה — בלי שום API חיצוני, ואפילו יותר קל מהתוכנית הקודמת:**

**1. Bootstrap לפי ז'אנר (יש מיד, לפני שיש נתונים בכלל):**

```typescript
const genreSessionDefaults = {
  roguelike:         { typicalMinutes: 15,  interruptible: true  },
  battleRoyale:      { typicalMinutes: 25,  interruptible: false },
  moba:              { typicalMinutes: 30,  interruptible: false },
  openWorldSandbox:  { typicalMinutes: 20,  interruptible: true  },
  openWorldStory:    { typicalMinutes: 40,  interruptible: false }, // ברירת מחדל זהיר
  turnBasedStrategy: { typicalMinutes: 60,  interruptible: false }, // "one more turn"
  jrpg:              { typicalMinutes: 45,  interruptible: false },
  puzzle:            { typicalMinutes: 10,  interruptible: true  },
  visualNovel:       { typicalMinutes: 20,  interruptible: true  },
  narrative_linear:  { typicalMinutes: 50,  interruptible: false },
  simulation:        { typicalMinutes: 20,  interruptible: true  },
}
```
זה כבר נותן תוצאות סבירות ביום הראשון, אפס תלות, אפס API.

**2. שיפור מהנתונים של המשתמשים עצמם (אין צורך "לשאול" בנפרד!):**
כבר מתכננים טיימר סשן + לוג (§3.4–3.5). **הנתון כבר נאסף לבד**:
- משך הסשן בפועל נלקח מהטיימר
- שאלה אחת נוספת בלוג המהיר: **"היית יכול לעצור מתי שרצית, או שהיית נעול עד נקודה מסוימת?"** (כן/לא — טאפ אחד)
- אחרי כמה עשרות סשנים על משחק — יש חציון אמיתי של `typical_session_minutes`
  ו־`interruptible_ratio` **שמבוסס על התנהגות אמיתית, לא על מה שהמפתח אומר**
- זה בעצם A/B מתמשך: ברירת המחדל לפי ז'אנר מוחלפת בהדרגה בנתון אמיתי

**3. נוסחת ההתאמה (מחליפה את `timeFit` הישן ב־§4.1):**

```typescript
function sessionFit(availableMinutes: number, game: GameSessionProfile): number {
  const { typicalMinutes, interruptible } = game

  if (interruptible) {
    // אפשר לשחק כל זמן שיש, ולעצור מתי שרוצים
    return availableMinutes >= typicalMinutes * 0.3 ? 1.0 : 0.6
  }

  // לא ניתן להפרעה - חייבים מספיק זמן לסיים סשן שלם, אחרת עדיף לא להתחיל
  if (availableMinutes >= typicalMinutes) return 1.0
  if (availableMinutes >= typicalMinutes * 0.7) return 0.4  // "יהיה צמוד"
  return 0.0  // אל תציע בכלל - זה הרגע שהמוצר באמת עוזר
}
```

**זה בעצם הרגע שבו PlayNight שווה יותר מכל מתחרה:** מישהו עם 20 דקות
פנויות לא יקבל המלצה על משימת GTA שנייה שנעולה 40 דקות. הוא יקבל
Hades. **אף tracker קיים לא עושה את זה בכלל** — הם לא ממדלים סשן,
רק "מה יש בבאקלוג".

---

### 4.5 מנוע הכיול (Calibration Engine) — איך אוספים נתונים בלי להעיק

**העיקרון:** כל שאלה היא טאפ אחד, אף פעם לא חובה, ומופיעה רק כשהיא
**עוזרת למישהו** (למשתמש עצמו או לקהילה). אנחנו לא בונים "סקר" —
אנחנו בונים מנגנון שהופך כל סשן משחק לנקודת מידע קטנה, בלי שהמשתמש
ירגיש שהוא "עובד בשביל האפליקציה".

#### מתי שואלים

מוסיפים **שאלת כיול אחת** (לא יותר!) למסך הלוג המהיר (§3.5), רק כאשר:
- למשחק הזה יש **פחות מ־15 דיווחים** במאגר (אחרי זה — יש מספיק מידע, מפסיקים לשאול)
- זו לא שאלת הכיול השלישית ברצף לאותו משתמש היום (חסם עייפות)
- המשתמש לא סימן "אל תשאל אותי יותר" (§4.5.4)

ברגע שמשחק עובר את הסף → הוא "מכויל", והשאלות עליו נעלמות
**לכל המשתמשים**, גם למי שרק עכשיו מתקין את האפליקציה. ככה
כל משתמש חדש נהנה מהעבודה של הקודמים לו.

#### בנק השאלות (רוטציה — לא אותה שאלה כל פעם)

כל שאלה: כפתור אחד, 2–4 אופציות, טאפ = תשובה = סיום.

| # | שאלה | אופציות | מה זה מזין |
|---|---|---|---|
| 1 | היית יכול לעצור מתי שרצית, או שהיית נעול עד נקודה מסוימת? | כן, בכל רגע / לא, הייתי נעול / תלוי | `interruptible` |
| 2 | כמה זמן בערך אורך "פרק" טיפוסי במשחק הזה (לא הסשן הזה — באופן כללי)? | עד 15 דק' / 15–30 / 30–60 / שעה+ | `typical_session_minutes` |
| 3 | כמה תכופות נקודות השמירה במשחק הזה? | כל כמה דקות / רק בין פרקים / נדיר מאוד | תוסף ל־interruptible |
| 4 | זה היה יותר מתאים ל"קפיצה מהירה" או ל"ישיבה ארוכה"? | קפיצה מהירה / ישיבה ארוכה / שניהם עובדים | ולידציה הפוכה, תיוג מהיר |
| 5 | התיוג שלנו למשחק הזה (⚡ אקשן מהיר) הרגיש מדויק? | כן / לא, זה יותר [בחירה מרשימה] | מתקן `genre_mood_mapping` שגוי |

**חשוב:** שאלה #1 היא הכי חשובה וקריטית (מזינה ישירות את `sessionFit`
מ־§4.4). שאלות 2–5 הן שיפור/דיוק. תתחיל אך ורק עם #1 ב-MVP,
תוסיף את השאר רק אם יש זמן.

#### לוגיקת בחירת שאלה

```typescript
function pickCalibrationQuestion(game: Game, user: User): Question | null {
  if (game.session_reports_count >= 15) return null       // מכויל, אין צורך
  if (user.calibrationQuestionsToday >= 3) return null     // חסם עייפות יומי
  if (user.optedOutOfCalibration) return null

  // שאלה #1 קודמת לכולן עד שיש לה לפחות 8 תשובות
  if (game.interruptible_reports_count < 8) return QUESTION_1
  return pickWeightedRandom([Q2, Q3, Q4, Q5], weightedByGapInData)
}
```

#### תמריץ (קליל, לא מאולץ)

בלי נקודות/לבבות/gamification כבד — זה מרגיש מתחת למותג. **תמריץ אחד
בלבד, כן/אמין:**
לאחר תשובה → הצג לרגע: **"תודה — זה עוזר ל־[X] גיימרים אחרים לקבל
המלצה טובה יותר על המשחק הזה"** (X = ספירה אמיתית מהמאגר אם קיימת,
אחרת השמט את המספר). שקיפות במקום תגמול מלאכותי.

#### "תקופת ההרצה" (Beta Calibration Period)

בחודש-חודשיים הראשונים אחרי ההשקה, **כל המשחקים** במעשה חסרי נתונים
— כך שכמעט כל סשן יזכה לשאלת כיול. זה בסדר וזה מצופה: קבוצת ה-20
בודקים הראשונים (§8, שלב 5) היא בדיוק מי שבונה את הבסיס למאגר.
כדאי להגיד להם את זה במפורש בגיוס: **"האפליקציה הזאת לומדת ממך —
בשבועות הראשונים תראה יותר שאלות קטנות, וזה הופך אותה לחכמה יותר
לכולם אחריך."** שקיפות כזו הופכת את הבקשה לנתונים ל-feature, לא לבאג.

#### 4.5.4 יציאה מהמנגנון

בהגדרות: **"תפסיק לשאול אותי שאלות כיול"** — מכבד את זה מיד ולצמיתות
(אבל עדיין נהנה מהנתונים שאחרים תרמו). אל תסתיר את זה עמוק בתפריט —
משתמש שמרגיש נדחק לפינה יעזוב את כל האפליקציה, לא רק את השאלות.

---

## 5. מודל עסקי

### חינם
- עד **50 משחקים** בספרייה
- חיבור פלטפורמה **אחת**
- 3 המלצות בסוואיפ
- הערות "איפה עצרתי" — עד 20
- פרסומות באנר בתחתית הספרייה (**לא** במסך ההחלטה — זה קדוש)

### PlayNight Pro — 3.99$ / חודש או 24.99$ / שנה
- ספרייה ללא הגבלה
- כל הפלטפורמות
- המלצות ללא הגבלה + פילטרים מתקדמים
- הערות ללא הגבלה + צילומי מסך
- **Game Night** (תיאום עם חברים)
- סטטיסטיקות: כמה שעות שיחקת, כמה סיימת השנה, "ערך הבאקלוג"
- ווידג'ט למסך הבית
- ללא פרסומות

**הערה על תמחור:** 3.99$/חודש מתחרה נכון מול GG שגובה 4.99$.
המחיר השנתי הוא העיקר — כוון לשם.

**מתי לבקש תשלום:** אחרי שהמשתמש **סיים** את הסשן הראשון שלו,
לא לפני. הרגע שבו הוא מרגיש שזה עבד.

---

## 6. מודל נתונים

```sql
-- SQLite מקומי + מסונכרן ל-Supabase

users (
  id, email, created_at, is_pro,
  default_session_minutes, primary_platforms JSON
)

games (                    -- catalog משותף, cached
  id, igdb_id, name, cover_url, release_year,
  genres JSON, platforms JSON, community_rating,
  typical_session_minutes,   -- מתחיל מברירת מחדל לפי ז'אנר, מתעדכן מנתוני משתמשים
  interruptible,             -- BOOLEAN, אותו דבר
  session_reports_count,     -- כמה סשנים אמיתיים כבר עדכנו את הנתון הזה
  interruptible_reports_count,  -- כמה תשובות ספציפית לשאלת הכיול #1 (§4.5)
  is_calibrated              -- BOOLEAN, TRUE כש-session_reports_count >= 15
)

calibration_answers (        -- כל תשובה לשאלת כיול, גולמית (§4.5)
  id, user_id, game_id, session_id,
  question_id,                -- 1-5, ראה בנק השאלות
  answer_value, answered_at
)

user_games (
  id, user_id, game_id,
  status,                  -- playing | backlog | beaten | completed | shelved | abandoned
  platform,                -- באיזו פלטפורמה העותק הזה
  added_at, last_played_at,
  hours_played,
  progress_percent,
  is_hidden BOOLEAN,
  dismissed_until DATE     -- אחרי swipe שמאלה
)

sessions (
  id, user_id, game_id,
  started_at, ended_at, duration_minutes,
  mood_before,             -- מה בחר במסך הבית
  rating,                  -- 😍🙂😐😴
  stopped_note TEXT,       -- "איפה עצרתי"
  could_stop_anytime,      -- BOOLEAN — הנתון שמזין את session fit (§4.4)
  screenshot_url
)

recommendations_log (      -- לשיפור המנוע
  id, user_id, game_id, shown_at,
  score, action             -- accepted | dismissed | hidden
)

friends (user_id, friend_id, status)
game_nights (id, host_id, scheduled_at, participants JSON, chosen_game_id)
```

---

## 7. סיכונים — קרא את זה לפני שאתה מתחיל

| סיכון | חומרה | מה לעשות |
|---|---|---|
| ~~HowLongToBeat API~~ | ✅ **לא רלוונטי** | **הרעיון המקורי היה שגוי מהיסוד** — "זמן עד הסוף" זה לא מה שצריך. הוחלף ב"פרופיל סשן" (§4.4): כמה זמן לוקח סשן טיפוסי + האם ניתן להפרעה. נבנה מברירות מחדל לפי ז'אנר ומשתפר מנתוני המשתמשים עצמם. אפס תלות חיצונית שבירה, ובנוסף — פיצ'ר ייחודי שאף מתחרה לא עושה. |
| PSN/Nintendo אין API | 🟡 בינוני | MVP: ידני בלבד. אל תבטיח מה שאין. |
| השוק צפוף יותר ממה שנראה | 🟡 בינוני | ההבדלה חייבת להיות **חדה**: אנחנו לא tracker. כל הודעת שיווק מדברת על ההחלטה. |
| בעיית "ספרייה ריקה" | 🟡 בינוני | ייבוא Steam חייב לעבוד מושלם. משתמש שמזין 200 משחקים ידנית — נוטש. |
| IGDB rate limits | 🟢 נמוך | 4 בקשות/שנייה. Cache אגרסיבי ב־Supabase. |
| אתה מפתח יחיד עם ניסיון בסיסי | 🟡 בינוני | **תשלח MVP צר.** אל תבנה את §3.8 בגרסה ראשונה. |

---

## 8. תוכנית בנייה — סדר עבודה ל־Claude Code

### שלב 0 — אימות (לפני שורת קוד אחת) — יום אחד
- [ ] הוצא מפתח IGDB (דרך Twitch Developer Console)
- [ ] בדוק ש־Steam API מחזיר את הספרייה שלך (Steam ID + `GetOwnedGames`)
- [ ] ~~HLTB~~ — לא רלוונטי יותר, הוסר מהתוכנית

### שלב 1 — שלד — שבוע 1
- [ ] `npx create-expo-app --template` + TypeScript + Expo Router
- [ ] NativeWind, Zustand, SQLite + Drizzle
- [ ] מסכים ריקים עם ניווט ביניהם
- [ ] הוספת משחק **ידנית** + חיפוש IGDB

### שלב 2 — הליבה — שבוע 2–3
- [ ] ייבוא Steam
- [ ] מסך הבית (זמן + מצב רוח)
- [ ] **מנוע ההמלצה** (§4.1) — כפונקציה טהורה עם unit tests
- [ ] מסך Swipe עם Reanimated

### שלב 3 — הלולאה — שבוע 4
- [ ] טיימר סשן + מסך לוג מהיר
- [ ] "איפה עצרתי" + התראת החזרה
- [ ] **מנוע כיול (§4.5)** — שאלה #1 בלבד ל-MVP, לוגיקת סף 15 דיווחים
- [ ] הספרייה עם פילטרים

### שלב 4 — כסף — שבוע 5
- [ ] Supabase Auth + sync
- [ ] RevenueCat + מסך paywall
- [ ] PostHog

### שלב 5 — שחרור — שבוע 6
- [ ] TestFlight + Google Play Internal Testing
- [ ] 20 בודקים מ־r/patientgamers, r/gamingsuggestions — לגייס אותם
      במפורש כ"תקופת הרצה" (§4.5) שבונה את מאגר הנתונים לכולם
- [ ] צילומי מסך + טקסט לחנות
- [ ] **שחרור באנדרואיד קודם** — קל יותר, אישור מהיר יותר

---

## 9. הוראות ל־Claude Code

בתחילת כל סשן, תן ל־Claude Code את ההוראות האלה:

```
אנחנו בונים את PlayNight לפי SPEC.md.

כללים:
- TypeScript קפדני. אל תשתמש ב-any.
- כל קובץ מתחת ל-200 שורות. פצל לרכיבים.
- מנוע ההמלצה (lib/recommendation.ts) הוא פונקציה טהורה עם טסטים.
- הספרייה עובדת offline — SQLite הוא מקור האמת, Supabase הוא sync.
- אל תבנה פיצ'רים שלא ב-SPEC. אם יש רעיון — תרשום ב-IDEAS.md.
- אחרי כל שלב: הרץ את האפליקציה ותוודא שהיא עולה לפני שממשיכים.
- מסך ההחלטה חייב להרגיש מיידי. אין ספינרים בזרימה הזאת.

התחל משלב 1 ב-SPEC.md §8.
```

---

## 10. איך יודעים שזה עובד

| מדד | יעד |
|---|---|
| זמן מפתיחה עד לבחירת משחק | < 30 שניות |
| אחוז המלצות שהתקבלו (swipe ימינה) | > 40% |
| שימור יום 7 | > 25% |
| שימור יום 30 | > 12% |
| המרה לפרו | 2–4% |
| סשנים בשבוע למשתמש פעיל | > 3 |

**המדד היחיד שבאמת חשוב:** האם משתמשים חוזרים בשבוע השני?
אם לא — הבעיה היא במנוע ההמלצה, לא בעיצוב.

---

## 11. רעיונות לגרסאות עתידיות (אל תבנה עכשיו)

- ווידג'ט iOS/Android: "מה לשחק" ישירות ממסך הבית
- אינטגרציה עם Discord Rich Presence
- "שנה בגיימינג" — סיכום שנתי כמו Spotify Wrapped (ויראלי!)
- התראה על משחקים מהבאקלוג שירדו במחיר
- מצב "בן/בת זוג" — משחקי co-op לשניים
- ייבוא הישגים והצגת "3 הישגים מהסיום"
