# PlayNight — פרומפטים ל-Claude Code

> **לפני שמתחילים:** שים את `PLAYNIGHT_SPEC.md` בתיקיית הפרויקט (בשם `SPEC.md`).
> תן לכל פרומפט להסתיים (כולל בדיקה שהאפליקציה עולה) לפני שעוברים לבא.
> אם קלוד קוד נתקע או סוטה מהתוכנית — תפנה אותו בחזרה ל-`SPEC.md`, לא תנחה אותו מהזיכרון.

---

## פרומפט 0 — הגדרה (להריץ פעם אחת, בתחילת הפרויקט)

```
אנחנו בונים אפליקציה בשם PlayNight. כל האפיון נמצא בקובץ SPEC.md
בתיקייה הזו — תקרא אותו עכשיו במלואו לפני שאתה עושה כל דבר אחר.

כללי עבודה קבועים לאורך כל הפרויקט:
1. TypeScript קפדני (strict mode). אסור להשתמש ב-any.
2. כל קובץ מתחת ל-200 שורות. אם קובץ מתנפח - תפצל לרכיבים/hooks/utils.
3. לוגיקה עסקית (מנוע ההמלצה, sessionFit, מנוע הכיול) חייבת להיות
   בפונקציות טהורות ב-lib/, נפרדות מה-UI, עם unit tests.
4. הספרייה חייבת לעבוד אופליין. SQLite הוא מקור האמת המקומי,
   Supabase הוא sync ברקע - לא תלות בזמן אמת.
5. אל תבנה שום פיצ'ר שלא מופיע ב-SPEC.md. אם עולה רעיון טוב באמצע
   העבודה - תרשום אותו בקובץ IDEAS.md ותמשיך במה שתוכנן.
6. מסך "מה לשחק?" (§3.2) ומסך ה-Swipe (§3.3) הם הלב של המוצר -
   אסור שיהיה בהם ספינר, עיכוב, או תחושת "טעינה". כל דבר אחר יכול
   להיות פחות מהודק.
7. בסוף כל שלב עבודה: תריץ את האפליקציה בפועל (iOS simulator או
   Android emulator) ותוודא שהיא עולה בלי קריסה, לפני שאתה ממשיך
   לשלב הבא. תגיד לי מפורשות "בדקתי, זה עובד" או תדווח על הבעיה.
8. אם משהו ב-SPEC לא ברור או נראה לא נכון טכנית - תעצור ותשאל אותי,
   אל תנחש ואל תמציא פתרון שסותר את המסמך.

עכשיו תקרא את SPEC.md §8 (תוכנית הבנייה) ותאשר לי שהבנת את סדר
השלבים. אל תתחיל לכתוב קוד עדיין - רק תאשר שקראת ותסכם לי בקצרה
מה המוצר עושה, במילים שלך.
```

---

## פרומפט 1 — שלב 0: אימות (יום אחד)

```
נעבור לשלב 0 מ-SPEC.md §8.

1. תעזור לי להוציא מפתח IGDB: תסביר לי בדיוק אילו צעדים לעשות
   ב-Twitch Developer Console כדי לקבל Client ID + Client Secret
   לשימוש ב-IGDB API. אני אבצע את זה ידנית ואכניס את המפתחות
   ל-.env - אתה תכין את קובץ ה-.env.example המתאים.

2. תכתוב לי סקריפט קטן (Node script, לא חלק מהאפליקציה עדיין) שבודק
   קריאה בסיסית ל-Steam Web API (GetOwnedGames) עם Steam ID לדוגמה,
   כדי שנוודא שהגישה עובדת לפני שבונים סביבה עליה.

3. תוודא שיש לנו Node, Expo CLI, ו-Watchman (אם macOS) מותקנים
   ומעודכנים, ותגיד לי אם חסר משהו.

אל תתקדם לשלב 1 עד שאני מאשר שהמפתחות עובדים ושהסקריפט של Steam
מחזיר תוצאה תקינה.
```

---

## פרומפט 2 — שלב 1: שלד (שבוע 1)

```
שלב 0 הושלם. נעבור לשלב 1 מ-SPEC.md §8: השלד.

1. הרם פרויקט Expo חדש עם TypeScript, בתצורת Development Build
   (לא Expo Go - צריך native modules בהמשך לפי §2 ב-SPEC).
2. התקן והגדר: Expo Router (file-based navigation), NativeWind,
   Zustand, expo-sqlite + Drizzle ORM.
3. תבנה את מבנה התיקיות הבסיסי לפי המסכים ב-SPEC.md §3:
   onboarding, home ("מה לשחק"), swipe, session-confirm, quick-log,
   library, game-detail. כרגע כל מסך יכול להיות placeholder ריק עם
   כותרת וניווט תקין ביניהם - אין צורך בלוגיקה עדיין.
4. תגדיר את סכמת ה-SQLite הראשונית לפי טבלאות games ו-user_games
   מ-SPEC.md §6 (עדיין בלי sessions/calibration_answers - זה שלב 3).
5. תבנה מסך "הוסף משחק ידנית" עם חיפוש מול IGDB API (§4.3 קטגוריית
   "הוספה ידנית") - זו הדרך הראשונה שנוכל לבדוק שהכל מחובר קצה לקצה.

בסיום: תראה לי איך זה נראה (או תריץ ותתאר לי את הזרימה), ותוודא
שאפשר לחפש משחק ב-IGDB ולהוסיף אותו לספרייה המקומית ב-SQLite.
```

---

## פרומפט 3 — שלב 2: הליבה (שבוע 2–3)

```
שלב 1 הושלם ועובד. נעבור לשלב 2 מ-SPEC.md §8: הליבה.

חשוב: תבנה את זה בסדר הזה בדיוק, כל חלק נבדק לפני המעבר לבא:

1. ייבוא Steam (§4.3): מסך שמקבל Steam ID, קורא ל-GetOwnedGames,
   ומייבא את הספרייה ל-SQLite (טבלת user_games). תתמודד עם ספריות
   גדולות (200+ משחקים) בלי לתקוע את ה-UI - טעינה ברקע עם progress
   indicator.

2. מסך הבית "מה לשחק?" (§3.2): בחירת זמן פנוי + מצב רוח, לפי
   העיצוב המדויק במסמך. שמור את הבחירות ב-Zustand, לא ב-state מקומי
   של הקומפוננטה - נצטרך אותן במסך הבא.

3. מנוע ההמלצה (§4.1 + §4.4 + §4.5): תבנה את זה כפונקציה טהורה
   ב-lib/recommendation.ts עם unit tests (Jest). זה הכי קריטי בכל
   האפליקציה - תקרא שוב את §4.1 ו-§4.4 לפני שאתה כותב, ותוודא
   שהנוסחה כולל את sessionFit בדיוק כמו שמתואר (לא timeFit הישן -
   זה הוחלף). תכתוב טסטים שבודקים גם את המקרה של game לא-מכויל
   (בלי session data) וגם מכויל.

4. מסך Swipe (§3.3): 3-5 כרטיסים מבוססי מנוע ההמלצה, אנימציית
   swipe עם Reanimated 3 + react-native-gesture-handler (לא
   Animated הישן - זה קריטי לביצועים). וודא שהמחוות (ימינה/שמאלה/
   למעלה) עובדות בדיוק כמו בטבלה ב-§3.3.

בסיום כל חלק - תריץ ותבדוק בפועל, לא רק "אמור לעבוד". דווח לי
בקצרה מה נבדק ומה התוצאה.
```

---

## פרומפט 4 — שלב 3: הלולאה (שבוע 4)

```
שלב 2 הושלם. נעבור לשלב 3 מ-SPEC.md §8: לולאת השימוש.

1. הוסף לסכמת ה-SQLite את טבלאות sessions ו-calibration_answers
   מ-§6 המעודכן.

2. מסך אישור + טיימר סשן (§3.4): טיימר פשוט שרץ ברקע (גם אם
   האפליקציה במינימום), עם התראה עדינה בסיום הזמן שנבחר. שדה
   "איפה עצרתי" חופשי.

3. מסך לוג מהיר (§3.5): חייב להיות ניתן למילוי בפחות מ-15 שניות.
   כלול את שאלת הכיול (§4.5) - רק שאלה #1 בשלב הזה ("היית יכול
   לעצור מתי שרצית?"), עם הלוגיקה שמחליטה מתי להציג אותה
   (game.session_reports_count < 15, חסם 3 שאלות ליום). תעדכן את
   game.interruptible ו-typical_session_minutes בכל תשובה לפי
   הנוסחה ב-§4.4.

4. התראת חזרה: אם משתמש פותח משחק שלא נגע בו >30 יום, הצג את
   ההערה האחרונה שלו ("איפה עצרתי") כמו שמתואר ב-§4.2. זה הפיצ'ר
   שאתה יודע שהוא ה-USP המרכזי - תוודא שזה עובד חלק ומיד, לא
   בעיכוב.

5. מסך הספרייה (§3.6) + מסך משחק בודד (§3.7) עם כל הפילטרים
   וההיסטוריה שמתוארים במסמך.

תבדוק בסוף: לך פיזית דרך המסע השלם - בחר זמן, קבל המלצה, "שחק",
עצור, מלא לוג, וודא שההערה נשמרת ושהיא מופיעה כשחוזרים למשחק.
```

---

## פרומפט 5 — שלב 4: כסף (שבוע 5)

```
שלב 3 הושלם. נעבור לשלב 4 מ-SPEC.md §8: מודל עסקי.

1. Supabase: הגדר פרויקט (אני כבר יצרתי אחד / תדריך אותי איך
   ליצור), חבר Auth (email + Google/Apple sign-in), ותבנה סנכרון
   ברקע בין SQLite המקומי ל-Postgres - SQLite נשאר מקור האמת,
   Supabase הוא backup + הבסיס לנתוני הכיול המשותפים (§4.5).
   שים לב: טבלת games (כולל typical_session_minutes,
   session_reports_count) חייבת להיות משותפת בין כל המשתמשים,
   לא per-user - זה הלב של יתרון הנתונים שלנו.

2. RevenueCat: הגדר לפי מודל המנוי ב-§5 (חינם עד 50 משחקים /
   פלטפורמה אחת, Pro ב-3.99$/חודש או 24.99$/שנה). תבנה את מסך
   ה-paywall - חשוב: הוא מופיע אחרי שהמשתמש סיים סשן ראשון
   בהצלחה, לא לפני (§5, "מתי לבקש תשלום"). תוסיף gating בקוד
   לפי הגבלות החינמי.

3. PostHog: הטמע מעקב לפי המדדים ב-§10 (זמן עד בחירה, אחוז
   swipe ימינה, שימור). אל תעקוב אחרי יותר מזה - פרטיות המשתמש
   חשובה ואנחנו לא צריכים אנליטיקס אגרסיבי.

תבדוק שכל הזרימה עובדת: הרשמה, סנכרון, ופתיחת ה-paywall בזמן הנכון.
```

---

## פרומפט 6 — שלב 5: שחרור (שבוע 6)

```
שלב 4 הושלם. נעבור לשלב 5 מ-SPEC.md §8: הכנה לשחרור.

1. תעבור על כל האפליקציה ותכין רשימת checklist לפי §10 (המדדים) -
   וודא שכל אירוע שאנחנו צריכים למדוד באמת נשלח ל-PostHog.

2. תכין את קבצי ה-config הנדרשים ל-EAS Build (Expo) עבור:
   - TestFlight (iOS)
   - Google Play Internal Testing (Android) - נתחיל מכאן קודם,
     לפי §8 שלב 5 ("שחרור באנדרואיד קודם")

3. תבדוק שיש לנו Privacy Policy ו-Terms of Service בסיסיים
   (תוכל לייצר טיוטה, אני אעביר על עורך דין/שירות מתאים לפני
   שחרור אמיתי) - חובה לחיבור חשבונות Steam/Google Sign-in.

4. תעזור לי לנסח טקסט לחנות (App Store + Google Play): כותרת,
   תיאור קצר, תיאור מלא, שמדגישים את ה-differentiator מ-SPEC.md
   §1 - "לא עוד tracker, אלא כלי החלטה".

5. תכין רשימת בדיקות ידניות (smoke test) שאני אעבור עליה פעם
   אחת ידנית לפני שאני שולח ל-20 הבודקים הראשונים.

אל תבנה שום פיצ'ר חדש בשלב הזה - רק ליטוש, בדיקות, והכנה לשחרור.
```

---

## טיפים לשימוש

- **פתח שיחה חדשה עם קלוד קוד לכל שלב**, לא המשך אחד ארוך - זה
  שומר על context נקי וממוקד.
- אם קלוד קוד מציע לסטות מה-SPEC ("בוא נוסיף גם...") - תחליט במודע,
  ואם אתה מסכים, תעדכן את `SPEC.md` בעצמך (או תבקש ממני) לפני
  שממשיכים, כדי שהמסמך יישאר מקור האמת.
- כל שינוי משמעותי בתוכנית (כמו מה שעשינו עם HLTB ו-session fit) -
  תעדכן קודם את `SPEC.md`, ורק אז תן פרומפט חדש לקלוד קוד.
